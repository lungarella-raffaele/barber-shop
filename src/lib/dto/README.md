# Data transfer object

The current folder contains all the type definitions returned by the backend services. These are mappings to the real domain stripped down of sensitive data that should not be used nor seen on the client.
